<div id="<%= dom_id client %>">
  <tr>
    <th scope="row"><%= client.id %></th>
    <td><%= client.name %></td>
    <td><%= client.age %></td>
    <% if !(client.movie.blank?) %>
                        <td><%= client.movie.name %></td>
                <% else %>
                        <td><%= '*Ningún arriendo vigente*' %></td>
                <% end %>
    <td><%= link_to "Show this client", client %></td>
    <td><%= link_to "Edit this client", edit_client_path(client) %></td>
    <td><%= button_to "Destroy this client", client, method: :delete, :class=>"btn btn-dark" %></td>
  </tr>
</div>
